import SwapStationDraft from "@/components/drafts/SwapStationDraft";

export const metadata = { title: "Swap Station" };

export default function SwapStationOverviewPage() {
  return <SwapStationDraft />;
}
